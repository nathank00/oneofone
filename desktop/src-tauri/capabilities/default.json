{
  "identifier": "default",
  "description": "Default capabilities for the ONE OF ONE desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
